

export const TitleSection = ({children}) => {
    return (
        
        <h2 
            // className={ 'leading-none font-bold'}
        >
        
            {children}
        </h2>
    )
}
