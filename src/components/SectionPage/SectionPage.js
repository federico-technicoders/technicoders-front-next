

export const SectionPage = ({children}) => {
    return (
        <section  className="relative flex justify-start items-start w-full  text-white bg-[#0E1629] -z-0">
            {children}
        </section>
    )
}
